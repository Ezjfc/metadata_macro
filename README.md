# Structfield Metadata
Rust macros to add any kind of metadata to the fields in your structs and tuple structs.

# Known issues
- Generic type parameters cannot be used in structs wrapped in `metadata!` or `metadata_only!` macro.
